// This file intentionally does nothing — it exists to stop tools from
// wandering up the directory tree looking for someone else's config.
// Boundaries are important. Even for slop.
module.exports = { plugins: [] };
